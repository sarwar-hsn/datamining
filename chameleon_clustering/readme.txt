#downlaod worms dataset at first

#install metis wrapper
-sudo apt-get install libmetis-dev
#install requirements file
-pip install -r requirements.txt
#run the program
-python main.py
#to configure different setting of the program check the initial section of the main.py
